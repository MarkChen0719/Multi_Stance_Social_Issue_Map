# 議題資料格式說明 (Schema)

本文件說明「多立場社會議題地圖」專案中議題 JSON 檔案的資料格式。如果你想要新增一個新的議題，請參考本文件來建立符合格式的 JSON 檔案。

## 檔案位置

所有議題 JSON 檔案都應該放在 `data/` 資料夾中，檔名格式為 `{議題ID}.json`。例如：`housing.json`、`ai_ethics.json`。

## 頂層結構

每個議題 JSON 檔案包含以下六個頂層欄位：

| 欄位 | 型態 | 必填 | 說明 |
|------|------|------|------|
| `id` | 字串 (String) | ✅ | 議題的唯一識別碼，建議使用英文小寫與連字號，例如：`housing`、`ai-ethics` |
| `title` | 字串 (String) | ✅ | 議題的標題，會顯示在頁面上方 |
| `description` | 字串 (String) | ✅ | 議題的詳細描述，約 2-3 句話，說明議題背景與探討面向 |
| `stances` | 陣列 (Array) | ✅ | 不同立場的列表，至少需要 3 個立場 |
| `nodes` | 陣列 (Array) | ✅ | 關鍵詞節點的列表，建議至少 10 個節點 |
| `links` | 陣列 (Array) | ✅ | 節點之間的連結關係列表 |

## 詳細欄位說明

### 1. `stances` 陣列

`stances` 是一個物件陣列，每個物件代表一個立場。建議至少包含 3 個立場，例如：政府、企業、民眾等。

#### 物件結構

| 欄位 | 型態 | 必填 | 說明 |
|------|------|------|------|
| `id` | 字串 (String) | ✅ | 立場的唯一識別碼，建議使用英文小寫，例如：`government`、`youth`、`developer` |
| `name` | 字串 (String) | ✅ | 立場的名稱，會顯示在立場卡片上，例如：「政府／主管機關」、「青年租屋族」 |
| `summary` | 字串 (String) | ✅ | 立場的摘要說明，約 3-4 句話，說明該立場如何看待議題、關注點與主要訴求 |

#### 範例

```json
{
  "stances": [
    {
      "id": "government",
      "name": "政府／主管機關",
      "summary": "政府認為高房價問題需要透過多管齊下的政策工具來處理，包括增加社會住宅供給、實施囤房稅與實價登錄制度、打擊炒房行為，同時透過租金補貼協助弱勢租屋族。政策設計必須兼顧市場穩定與金融秩序，避免過度干預導致投資信心下滑或房市劇烈波動。"
    },
    {
      "id": "youth",
      "name": "青年租屋族或首購族",
      "summary": "青年世代面臨低薪資、高房價的雙重壓力，租金負擔沉重，購屋遙不可及。他們普遍期待政府更積極打炒房、提高囤房稅與持有成本，大幅增加社會住宅供給，並改善租屋市場資訊透明度與法律保障，讓長期租屋也能維持基本居住安全與生活品質。"
    }
  ]
}
```

### 2. `nodes` 陣列

`nodes` 是一個物件陣列，每個物件代表一個關鍵詞節點。這些節點會顯示在網絡圖中，並可依群組分類。

#### 物件結構

| 欄位 | 型態 | 必填 | 說明 |
|------|------|------|------|
| `id` | 字串 (String) | ✅ | 節點的唯一識別碼，建議使用英文小寫與連字號，例如：`high-price`、`algorithm-bias` |
| `label` | 字串 (String) | ✅ | 節點的顯示名稱，會顯示在網絡圖上，例如：「高房價」、「演算法偏見」 |
| `group` | 字串 (String) | ✅ | 節點所屬的群組，通常對應到某個立場的 `id`，或使用 `neutral` 表示中立。例如：`gov`、`youth`、`developer`、`neutral` |
| `explanation` | 字串 (String) | ⚠️ 建議填寫 | 節點的詳細說明，約 2-3 句話，解釋該關鍵詞的意義與重要性。如果沒有填寫，說明區塊會隱藏 |
| `examples` | 字串陣列 (Array of Strings) | ⚠️ 建議填寫 | 節點的例句列表，建議提供 2 個例句，展示該關鍵詞在實際情境中的使用。如果沒有填寫，例句列表會隱藏 |

#### 範例

```json
{
  "nodes": [
    {
      "id": "high-price",
      "label": "高房價",
      "group": "neutral",
      "explanation": "整個議題的核心問題，指房價相對於收入與生活成本過高，造成多數家庭難以負擔。",
      "examples": [
        "台北市高房價讓許多年輕人覺得一輩子都買不起房。",
        "高房價已經影響到企業招募與人口流動。"
      ]
    },
    {
      "id": "social-housing",
      "label": "社會住宅",
      "group": "gov",
      "explanation": "由政府或公辦民營提供、以合理租金為主的住宅，主要服務中低收入或弱勢族群，被視為改善居住正義的重要工具。",
      "examples": [
        "政府承諾在未來數年內大幅增加社會住宅的戶數。",
        "不少青年認為社會住宅申請門檻仍然偏高。"
      ]
    }
  ]
}
```

#### 關於 `group` 欄位

`group` 欄位用來將節點分類，通常會對應到某個立場的 `id`。例如：
- 如果立場的 `id` 是 `government`，相關節點的 `group` 可以是 `gov` 或 `government`
- 如果立場的 `id` 是 `youth`，相關節點的 `group` 可以是 `youth`
- 使用 `neutral` 表示該關鍵詞屬於中立或跨立場的概念

系統會根據 `group` 來決定節點在網絡圖中的顏色，並在列表模式中進行分類顯示。

### 3. `links` 陣列

`links` 是一個物件陣列，每個物件代表兩個節點之間的連結關係。這些連結會顯示在網絡圖中，形成關鍵詞之間的關聯網絡。

#### 物件結構

| 欄位 | 型態 | 必填 | 說明 |
|------|------|------|------|
| `source` | 字串 (String) | ✅ | 來源節點的 `id`，必須對應到 `nodes` 陣列中某個節點的 `id` |
| `target` | 字串 (String) | ✅ | 目標節點的 `id`，必須對應到 `nodes` 陣列中某個節點的 `id` |
| `weight` | 數字 (Number) | ✅ | 連結的權重，表示兩個關鍵詞之間的關聯強度，建議範圍在 0.6 到 0.95 之間。權重越高，表示關聯越強，在網絡圖中連線會越粗 |

#### 範例

```json
{
  "links": [
    { "source": "high-price", "target": "purchase-pressure", "weight": 0.95 },
    { "source": "low-salary", "target": "rent-burden", "weight": 0.85 },
    { "source": "social-housing", "target": "rent-burden", "weight": 0.75 },
    { "source": "algorithm-bias", "target": "fairness", "weight": 0.95 }
  ]
}
```

#### 關於連結權重

- **0.9 - 0.95**：非常強的關聯，例如核心概念之間的直接關係
- **0.8 - 0.89**：強的關聯，例如重要概念之間的關係
- **0.7 - 0.79**：中等的關聯，例如相關但非直接的概念
- **0.6 - 0.69**：較弱的關聯，例如間接或次要的關係

## 完整範例

以下是一個簡化的完整範例，展示所有欄位的結構：

```json
{
  "id": "example-issue",
  "title": "範例議題",
  "description": "這是一個範例議題，用來展示資料格式。請替換成你實際的議題內容。",
  "stances": [
    {
      "id": "government",
      "name": "政府立場",
      "summary": "政府認為需要透過政策工具來解決問題，同時兼顧市場穩定與社會公平。"
    },
    {
      "id": "citizen",
      "name": "民眾立場",
      "summary": "民眾關注自身權益與生活品質，期待政府提供更多保障與支持。"
    }
  ],
  "nodes": [
    {
      "id": "core-concept",
      "label": "核心概念",
      "group": "neutral",
      "explanation": "這是議題的核心概念，影響所有立場。",
      "examples": [
        "核心概念在實際情境中的應用範例一。",
        "核心概念在實際情境中的應用範例二。"
      ]
    },
    {
      "id": "policy-tool",
      "label": "政策工具",
      "group": "government",
      "explanation": "政府用來解決問題的政策工具。",
      "examples": [
        "政策工具的使用範例一。",
        "政策工具的使用範例二。"
      ]
    }
  ],
  "links": [
    { "source": "core-concept", "target": "policy-tool", "weight": 0.85 }
  ]
}
```

## 注意事項

1. **JSON 格式**：請確保檔案是有效的 JSON 格式，可以使用線上 JSON 驗證工具檢查。

2. **ID 唯一性**：
   - 議題的 `id` 必須是唯一的，不能與其他議題重複
   - 每個立場的 `id` 在該議題內必須唯一
   - 每個節點的 `id` 在該議題內必須唯一

3. **連結的有效性**：
   - `links` 中的 `source` 和 `target` 必須對應到 `nodes` 中實際存在的節點 `id`
   - 如果連結指向不存在的節點，網絡圖可能無法正常顯示

4. **節點數量建議**：
   - 建議至少提供 10 個節點，以確保網絡圖有足夠的內容
   - 系統會自動選出最重要的前 10 個節點顯示在網絡圖中（可調整），但列表模式會顯示所有節點

5. **立場數量建議**：
   - 建議至少提供 3 個立場，以展現議題的多元觀點
   - 立場過少可能無法充分展現議題的複雜性

6. **文字內容**：
   - 所有文字內容請使用繁體中文
   - 建議文字適合高中生或大學生閱讀
   - `summary` 和 `explanation` 建議使用完整的句子，避免過於簡略

## 測試你的 JSON 檔案

建立 JSON 檔案後，你可以：

1. 將檔案放在 `data/` 資料夾中
2. 在 `index.html` 中新增對應的議題卡片連結
3. 開啟瀏覽器測試，確認所有內容都能正常顯示

如果遇到問題，請檢查：
- JSON 格式是否正確
- 所有必填欄位是否都有填寫
- `links` 中的節點 ID 是否都存在於 `nodes` 中

